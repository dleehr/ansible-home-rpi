---
- name: Prepare raspberry pi
  hosts: localhost
  gather_facts: no
  connection: local
  vars:
    install: no
    mount_state: mounted
    samba_state: started
  roles:
  - disk-mount
  - samba-service
